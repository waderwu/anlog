import requests

def replay(host):
    url = "http://%s{path}"%host
    headers = {headers}

    data = {data}
    params = {params}
    r = requests.{method}(url, data=data, params=params, headers=headers, timeout=5)

    return r.text


if __name__ == "__main__":
    print(replay("127.0.0.1"))